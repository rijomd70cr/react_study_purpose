import excelSlice from './Reducer/ExcelSlice';
import Routes from './Router';

const containers = {
    "reducer": excelSlice,
    "router": Routes,
    "moduleName": "Excel"
}

export default containers