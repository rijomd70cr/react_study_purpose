import ChatSlice from './Reducer/ChatSlice';
import Routes from './Router';

const containers = {
    "reducer": ChatSlice,
    "router": Routes,
    "moduleName": "ChatSystem"
}

export default containers